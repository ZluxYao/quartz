name: zluxyao-quartz_2

services:
  quartz:
    build:
      context: ${QUARTZ_PROJECT_PATH:-/vol1/1000/blog/quartz-site}
      dockerfile: Dockerfile
    image: zluxyao-quartz:watcher-v2
    pull_policy: build
    container_name: zluxyao-quartz
    restart: unless-stopped
    init: true
    environment:
      TZ: ${TZ:-Asia/Shanghai}
      CHOKIDAR_USEPOLLING: "${CHOKIDAR_USEPOLLING:-true}"
      CHOKIDAR_INTERVAL: "${CHOKIDAR_INTERVAL:-3000}"
      QUARTZ_CONFIG_PATH: /usr/src/app/runtime-config/quartz.config.yaml
      QUARTZ_STATS_DB_PATH: ${QUARTZ_STATS_DB_PATH:-/usr/src/app/data/site-stats.sqlite}
    ports:
      - "${QUARTZ_HTTP_PORT:-2222}:2222"
      - "${QUARTZ_WS_PORT:-2223}:3001"
    volumes:
      - type: bind
        source: ${QUARTZ_CONTENT_PATH:-/vol1/1000/blog/ZluxYao}
        target: /usr/src/app/content
        read_only: true
      - type: bind
        source: ${QUARTZ_CONFIG_DIR:-.}
        target: /usr/src/app/runtime-config
        read_only: true
      - type: bind
        source: ${QUARTZ_STATS_PATH:-./data}
        target: /usr/src/app/data
    healthcheck:
      test:
        - CMD
        - node
        - -e
        - >-
          fetch('http://127.0.0.1:2222/').then(() =>
          process.exit(0)).catch(() => process.exit(1))
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 45s
    stop_grace_period: 30s
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
